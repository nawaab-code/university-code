//Program to read two numbers, find its GCD and LCM using function with arguments and without a return type

#include <stdio.h>
#include <math.h>

void calculate (int a, int b)
{
    int gcm;
    if (a > b)
    {
        while(a%b==0)
        {
            
        }
    }
    
}
int main()
{
    return 0;
}